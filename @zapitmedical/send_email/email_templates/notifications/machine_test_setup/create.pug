extends ../../template.pug
block content
  tr
    td Facility
    td Machine
    td Test
  each t in transactions
    tr
      td #{t.machine.facility.name}
      td #{t.machine.name}
      td #{t.alias || t.test_version.label}
